spring:
  application:
    name: user-service
 # main:
 #   web-application-type: reactive
server:
  port: 8083
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}

